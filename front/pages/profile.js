import ProfileLayout from "../components/ProfileLayout";

const dummy = {
    nickname: 'insta',
    post: [],
    follower: [],
    following: [],
};

const Profile = () => {
    return (
        <ProfileLayout/>
    );
};

export default Profile;